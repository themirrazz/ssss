/*if(window.setInterval) {
    setInterval(function () {
        if(document.querySelector) {
            var h0 = document.querySelector('.header-left');
            var h1 = document.querySelector('.header-right');
            if(!isNaN(h0.offsetWidth) && !isNaN(window.innerWidth)) {
                document.querySelector('#hsep').style.width = (
                    window.innerWidth - (
                        h0.offsetWidth + h1.offsetWidth + 46
                    )
                ) + "px";
            }
        }
    });
}*/